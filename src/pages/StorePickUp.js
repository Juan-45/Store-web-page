import { Box } from "@mui/material";
import { Typography } from "@mui/material";

const StorePickUp = () => {
  return (
    <Box sx={{ height: "100vh", background: "gray", paddingTop: "50vh" }}>
      <Typography variant="h1">Buscar Sucursal</Typography>
    </Box>
  );
};

export default StorePickUp;
