import { styled } from "@mui/material/styles";
import Grid from "@mui/material/Grid";

//TODO set height 25vh solo en landscape
const BottomSection = styled(Grid)({
  height: "20vh",
  //height: "25vh", solo en landscape
  width: "80vw",
});

export default BottomSection;
