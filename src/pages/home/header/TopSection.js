import { styled } from "@mui/material/styles";
import Grid from "@mui/material/Grid";

const TopSection = styled(Grid)({
  //height 55vh solo en landscape
  height: "60vh",
  width: "80vw",
});

export default TopSection;
